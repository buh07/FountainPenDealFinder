# Source Adapters

Implement one adapter per marketplace:

- yahoo_auctions
- yahoo_flea_market
- mercari
- rakuma

Each adapter should conform to `ListingSourceAdapter` from `packages/common-types`.
